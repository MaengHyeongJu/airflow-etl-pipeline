[project]
name = "generator"
version = "0.1.0"
description = "Synthetic IoT sensor + application log data generator"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["generator*"]
